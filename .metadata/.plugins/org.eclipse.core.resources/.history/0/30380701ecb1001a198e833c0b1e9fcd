package model.domain;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.Id;

import lombok.Data;

@Data
@Entity
public class Board {
	@Id
	private Long no;
	@Column(length=10)
	private String gender;

	varchar(20) 
	finddate 유저가 셀렉트 박스로 타임을 선택하면 스틀링으로 집어넣을까요?

writer varchar(45) fk <<아 외래키 fk user의 id를 참조하는

loc  varchar(45)

detail varchar(45)
writedate date 타입 jpa

	활용 (temporay인가 그거)

}
